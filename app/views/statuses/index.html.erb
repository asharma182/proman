<div class="/navbar-inner">
    <div class="/container">
      <a class="/btn btn-navbar" data-toggle="collapse"
      data-target=".nav-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </a>
    <a class="brand" href="#">PMS</a>
    <div class="nav-collapse">
      <ul class="nav">
       <a>
       <!--<%= link_to h(image_tag 'rails.png'), {:controller => 'project',:action => 'index'}%>-->

         <%= link_to 'Project',{:controller => 'project',:action => 'index'} %>
         |
         <%= link_to 'Calender', {:controller => 'calendar',:action => 'index'}%>
         |
         <%= link_to 'My Blog',{:controller => 'blogs', :action => 'index'}%>
         |
          <%= link_to 'Edit your Account', {:controller => 'users', :action => 'edit'}%>
          |
          <%= link_to 'Status',{:controller => 'statuses',:action => 'index'} %>
          | 
        
      <% if user_signed_in?%>
      <!-- Signed in as--> <%= current_user.first_name + " " + current_user.last_name %><%= image_tag current_user.image_url(:thumb).to_s%> Not you?
       <%= link_to "Sign out", destroy_user_session_path, :method => 'delete' %>
           <% else %>
    <%= link_to "Sign up"%> or <%= link_to "sign in",new_user_session_path%>
      <%end%>
       </a>
     </ul>
     </div>
     </div>
     </div>
     </div>
<h1>Activities</h1>
<!--
<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @statuses.each do |status| %>
  <tr>
    <td><%= link_to 'Show', status %></td>
    <td><%= link_to 'Edit', edit_status_path(status) %></td>
    <td><%= link_to 'Destroy', status, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />-->
<table>
  <tr>
    <ul>
<% @project.each do |f| %>
<hr>
<li>  <%= f.project_name %><a> is created by </a><%= current_user.first_name + " " + current_user.last_name%> <%= f.created_at%> </li>
  <%end%>

 
<hr>
<ul>
<li><%= current_user.first_name + " " + current_user.last_name%> added new user 
   <% @add_users.each do |p|%>
      <%= p.add_more_user%>
        to 
    <%= p.created_at%> <%end%></li>
</ul>
</tr>
</table>
