 <%= render @discussion.parent if @discussion.parent %>

  


  <%= form_for @discussion  do |f| %>
  <%= f.select(:topic_id, @topics.collect {|s| [s.id]})%>
<%= f.text_field(:name) %>
    <%=f.hidden_field(:parent_id)%>
     <%= f.submit %>
<%end%>